apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  selector:
    app: microservices-blog
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
  type: ClusterIP  # Internal service, only accessible within cluster